// Array of available nodes to connect to
export const nodes = ["https://api.s0.b.hmny.io"];

const getNodeUrl = () => {
  // const randomIndex = random(0, nodes.length - 1)

  return "https://api.s0.b.hmny.io"; //`https://harmony-0-rpc.gateway.pokt.network`; //
};

export default getNodeUrl;
